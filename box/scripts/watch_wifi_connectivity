*/1 * * * * root curl --max-time 15 google.com --silent -o /dev/null || (echo "restarting network-manager due to no http response from google.com"; /etc/init.d/network-manager restart) 2>&1 | logger -t watch_wifi_connectivity
